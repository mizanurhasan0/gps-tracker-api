import { TCP_PORT, SOCKET_PORT, REST_API_PORT } from '../common/constants';

export const appConfig = {
  tcp: {
    port: TCP_PORT,
  },
  socket: {
    port: SOCKET_PORT,
  },
  rest: {
    port: REST_API_PORT,
  },
};
